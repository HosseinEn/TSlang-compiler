def int main() {
    var int x; # r0
    var int y = 110; # r1
    var int z = 110 + y; # r2

    x = 50;

    if (x < y) {
        x = 20;
    }
    else {
        y = 30;
    }

    #z = 100;
    z = hello(x, y);

    return x + y + z;
}

def int hello(int xxx, int y) {
    var int x = 0;

    printInt(x);
    var int user_inp = scan();

    while (user_inp > 5) {
        x = x + 1;
        user_inp = user_inp - 1;
        printInt(x);
    }

    xxx = 1000;
    xxx = xxx + 1;
    y = xxx;

    var int i;
    for (i = 5 to 10) {
        x = x + 1;
        printInt(x);
    }

    return x;
}


