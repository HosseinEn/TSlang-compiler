
def int main() {
    def int something() {
        print("hello");
        return 'sojid';
    }

    var int a;

    var vector A;
    A[0] = 1;
    A[1] = "diofj";
    A[2] = 3;


    var vector akkk = list(5);
    akkk[0] = 10;
    akkk[1] = 10;
    akkk[2] = 10;
    akkk[10] = 10;

    var str test_assign;
    print(test_assign);


    find(A, a);
    find(A);
    find(a, A);

    return 0;
}

def int test(str aaa) return 122;

def int find(vector A, int n) {
    var int k = 10;
    var int j = 0;

    for (i = 'sidj' to 5) {
        if (n == k) {
            return j;
        }
        j = j + 1;
    }

    for (i = 0 to 5) {
        if (n == k) {
            return j;
        }
        j = j + 1;
    }   

    return -1;
}

def null redf(str x) {
    print(x);
}
def null redf(int x) {
    x = x + 1;
}

def str something1() {
    var vector hel = 'ijoo';
    com(-2*8, first_arg, [second_arg, 1]);
    {
        var int hel = 5;
    }

    var int some = 3 + 4 * -5;
    if (halo == +2) 
        if (x == 2)
            print("aa");
    else
        if (x == 2)
            print ("hello");



}

def null  com(str x, str bo, int y) {
    var str something = "osijfoi";
    x = something * 2;
    a = "hello";

    var vector data = [1,2,3,5];

    if (y == 50) {
        for (j = 0 to use_as_loop_end_range()) {
            printInt(data[5]);
        }
        print("jjjj");
    }

    something = 34;
    {
        var int something = 100;
        something = something + 50;
    }
    var str something = "redeclared and error!";


    while('jfjf' {
        yy();
        oisjdoij();
        2 * 'jjj';
    }
}

def str use_as_loop_end_range() {
    return 'Hi!';
}




def null combination(int n, int r, int index, vector data, int i) { 

    var str x = "hello";
    if (i >= n) {
        #data[index] = i;
    }
    combination(n, r, index + 1, data, i + 1);
    # current is excluded, replace it with next (Note that
    # i+1 is passed, but index is not changed)
    combination(n, r, index, data, i + 1);
}


# int main (void) {
#     int x = a546f;
#     int A[54a] = 5;
#     'A' = x;
#     if (x 10 {
#         retur 5;
#     }
# }
# def int main() {
#     #var str text = "I was there. \\"yellow! I will be there\\" Sasha said. that was all."
# }



def null x98OI() {
    sum(ds oidf oidfj);
}


def int sum(vector numList) {
    var int result = 0;
    for (var int i = 0 to 5) {
        result = result + numList[5];
    }
    # returning the result.
    return result;
}

def str concatenate(int lengthOfString) {
    int i = 0;
    var vector b = [1, 2, 3, 4];
    print(x);
    # while (i < lengthOfString) {
    #     print(x);
    # }
}

# # recursive combination function

def null combination(int n, int r, int index, vector data, int i) {
    # Current combination is ready to be printed, print it
    if (index == r) {
        for (j = 0 to r) {
            print(data[j]);
        }
        print("jjjj");
        return ;
    }
    var str x = "hello";
    # When no more elements are there to put in data[]
    if (i >= n) {
        return;
    }
    # current is included, put next at next location
    data[index] = i;
    combination(n, r, index + 1, data, i + 1);
    # current is excluded, replace it with next (Note that
    # i+1 is passed, but index is not changed)
    combination(n, r, index, data, i + 1);
}

# def int max(int[] arr) {
#     int max_val = arr[0];
#     for (int i = 1; i < arr.length; i = i + 1) {
#         if (arr[i] > max_val) {
#             max_val = arr[i];
#         }
#     }
#     return max_val;
# }

# def str is_prime(int n) {
#     if (n <= 1) {
#         return 0;
#     }
#     for (var int i = 2; i <= n/2; i = i + 1) {
#         if (n % i == 0) {
#             return 0;
#         }
#     }
#     return 1;
# }


# def str reverse(int s) {
#     var str rev = "";
#     for (var int i = 6 - 1; i >= 0; i = i - 1) {
#         rev = rev + s[is_prime('jj')];
#     }
#     return rev;
# }



# def int dot_product(int[] a, int[] b) {
#     int sum = 0;
#     for (int i = 0; i < a.length; i = i + 1) {
#         sum = sum + a[i] * b[i];
#     }
#     return sum;
# }